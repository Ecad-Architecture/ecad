# Pending Tasks

- [ ] Add loading skeleton components for missing CMS data instead of using hardcoded image fallbacks. When `getWorkProjects` or other data queries are loading or return missing images, display a skeleton loader.
- [ ] Ensure all skeletons match the exact dimensions of their target components to prevent any Cumulative Layout Shift (CLS) and maintain a premium UX.
